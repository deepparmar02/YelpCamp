<% layout('layout/boilerplate') %>

    <body>

        <h1>
            <%= campground.title %>
        </h1>
        <h2>
            <%= campground.location %>
        </h2>

        <footer>
            <a href="/campground">All Campgrounds</a>
            <br>
            <a href="/campground/<%= campground._id %>/edit">Edit Campground</a>

            <form action="/campground/<%= campground._id %>?_method=DELETE" method="POST">
                <button>Delete Campground</button>
            </form>
        </footer>